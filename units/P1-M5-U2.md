---
id: P1-M5-U2
milestone: P1-M5
title: Finish session teardown and end-to-end delivery
depends_on:
  - P1-M5-U1
spec_refs:
  - REQ-P1-M5
owned_files:
  - DropShot/CaptureSessionCoordinator.swift
  - DropShot/CaptureOverlayWindowController.swift
  - DropShot/SelectionOverlayView.swift
  - DropShot/ScrollControlPanelController.swift
  - DropShot/ScrollCaptureController.swift
  - DropShot/ResultWindowController.swift
  - DropShotTests/CaptureSessionCoordinatorTests.swift
  - DropShot.xcodeproj/project.pbxproj
required_paths: []
required_tools:
  - xcodebuild
creates_paths: []
installs_tools: []
validation_commands:
  - xcodebuild -project DropShot.xcodeproj -scheme DropShot build
  - xcodebuild -project DropShot.xcodeproj -scheme DropShot -destination 'platform=macOS' test
requires_live_ops: false
reviewer_focus:
  - `Done`, `Cancel`, and `Esc` all clean up overlay windows, HUD state, monitors, and session state without leftovers.
  - The final handoff path is end-to-end complete and does not introduce extra behavior beyond the agreed V0.1 flow.
---
## Objective

Close the loop so the V0.1 user flow works end to end and tears down cleanly.

## In scope

- Route `Done` to final image delivery through the result window.
- Route `Cancel` and `Esc` to full session teardown with no leftover UI or monitors.
- Add coordinator-level tests for the key session finish and cancel paths.

## Non-goals

- Additional capture modes.
- Background persistence.
- Runtime-driven analytics or logging surfaces.

## Completion contract

- The V0.1 flow is complete: hotkey, drag selection, live scroll, `Done`, result preview.
- Cancellation paths clean up all temporary windows and listeners.
- The final milestone ships with build and test coverage for the coordinator cleanup path.
